from django.db import models
{% for definition in model.classes %}
class {{definition.name}}(models.Model):
	{% for attribute in definition.attributes %}
	{{attribute.name}} = models.{{attribute.type|typeDef}}
	{% endfor %}
{% endfor %}